type: dnf
repos:
  copr:
    - bieszczaders/kernel-cachyos
install:
  packages:
    - kernel-cachyos
    - kernel-cachyos-devel-matched
remove:
  packages:
    - kernel
    - kernel-core
    - kernel-modules
    - kernel-modules-core
    - kernel-modules-extra