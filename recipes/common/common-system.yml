modules:
  - type: kargs
    arch: x86_64
    kargs:
      - preempt=full
      - mitigations=off
      - nowatchdog

  - type: systemd
    system:
      enabled:
        - uupd.timer
        - dconf-update.service
      masked:
        - rpm-ostreed-automatic.timer