modules:
  - type: kargs
    arch: x86_64
    kargs:
      - preempt=full
      - mitigations=off
      - nowatchdog

  - type: systemd
    system:
      enabled:
        - bootc-automatic-updates.timer
      masked:
        - rpm-ostreed-automatic.timer