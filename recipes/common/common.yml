modules:

  - type: files
    files:
      - source: system
        destination: / # copies files/system/* (* means everything inside it) into your image's root folder /

  - from-file: packages/base-system.yml
  - from-file: packages/base-desktop.yml
  - type: initramfs
  - type: signing